apiVersion: v1
kind: Service
metadata:
  name: {{ include "www2.fullname" . }}
  labels:
    {{- include "www2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "www2.selectorLabels" . | nindent 4 }}
